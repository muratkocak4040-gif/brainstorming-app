[build]
  command = "npm run build"
  publish = "out"

[[plugins]]
  package = "@netlify/plugin-nextjs"

[build.environment]
  NODE_VERSION = "20.10.0" # 🔥 bu sürüm %100 destekleniyor
  NPM_FLAGS = "--legacy-peer-deps"

[functions]
  node_bundler = "esbuild"

